import LocalStorage from "@bonny-kato/localstorage";

export const lStorage = new LocalStorage("BRAVO-COCO-CMS");

export const lStorageKeys = {
    AUTH_USER: "auth-user",
    AUTH_TOKEN: "auth-token",
};
