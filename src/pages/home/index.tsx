import { Input } from "@/components/form-control";

const HomePage = () => {
    return (
        <section>
            home page <Input hasError />
        </section>
    );
};
export default HomePage;
